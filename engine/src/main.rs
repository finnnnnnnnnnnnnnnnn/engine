use std::fs;

mod dom;
mod webidl;
mod html;
mod hr_time;
use webidl::types;
use dom::events::event;

fn main() {
    // let html = fs::read_to_string("test.html").unwrap();
    // let dom = tl::parse(&html, tl::ParserOptions::default()).unwrap();
    // println!("{:#?}", dom);
}