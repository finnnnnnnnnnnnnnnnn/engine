[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
futures = { version = "0.3.31", features = ["executor"] }
wgpu = "25.0.0"
winit = { version = "0.30.10", features = ["wayland", "x11"] }
